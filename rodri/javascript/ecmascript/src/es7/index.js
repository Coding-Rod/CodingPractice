//  If Array includes
let numbers = [1,2,3,7,8];
let num = 9;

console.log(numbers.includes(num)? "Si":"No",`se encuentra el valor ${num}`);

// Pow
let base = 4;
let exponent = 3;
let result = base ** exponent;

console.log(result);