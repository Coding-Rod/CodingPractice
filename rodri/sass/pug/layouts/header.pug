- var items = [1,2,3,4,'a','b','c',5.5,6.6,,true,null]
header
    nav
        ul
            each val,index in items
                li(class="nav-"+val class=pag==index?"active":"")= index+ ':' + val